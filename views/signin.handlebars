<!DOCTYPE html>
<html>

<head>
    <title>Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"
        integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/styles.css" media="screen" title="no title" charset="utf-8">
    <link href="https://fonts.googleapis.com/css?family=Baloo" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.4.1.min.js" crossorigin="anonymous"></script>
</head>
<body>
    <script>
        $(document).ready(function (event) {

            $("#registerBtn").click(function (event) {
                event.preventDefault();
                window.location.href = "/signup";
            });
        });
    </script>
    <div class="container roundAuth">
        <div id="login" class="modal-dialog roundAuth jumbo">
            <div class="modal-content roundAuth round">
                <div class="modal-header authHeader">
                    <h1 class="authTitle">Task Master</h1>
                </div>
                <div class="authBody">
                    <form name="signin" id="modalForm signin" method="POST" action="signin">
                        <div class="form-group inputField">
                            <label for"email" class="inputText">Email Address:</label>
                            <input type="text" name="email" class="text form-control inputText" id="email-input"
                                placeholder="Enter e-mail address">
                        </div>
                        <div class="form-group inputField">
                            <label for="password" class="inputText">Password:</label>
                            <input type="password" name="password" class="form-control inputText" id="pass-input"
                                placeholder="Enter password">
                        </div>
                        <div>
                            <div class="row">
                                <div class="mx-auto">
                                    <input class="btn btn-lg btn-primary border border-0 authModalLoginBtn" type="submit" value="Login"/>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="authFooter">
                    <button id="registerBtn" class="btn btn-block border border-0 authModalRegisterBtn" data-dismiss="modal">Don't have an account? <span class="blue">Click here to Register</span></button>
                </div>
            </div>
        </div>
    </div>
    </div>
    <script>
        const url = new URL(window.location);
        const params = new URLSearchParams(url.search);
        if (params.has("failure")) {
            let p = document.createElement('p');
            let msg = document.createTextNode("Invalid username or password.");
            p.style.color = 'red';
            p.appendChild(msg);
            document.body.insertBefore(p, document.body.childNodes[0]);
        }
    </script>
</body>
</html>